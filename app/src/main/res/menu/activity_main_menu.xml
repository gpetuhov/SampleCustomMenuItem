<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--Not that we set actionLayout instead of ordinary icon-->
    <item
        android:id="@+id/activity_main_alerts_menu_item"
        android:title="Alerts"
        app:actionLayout="@layout/alerts_action_view"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/activity_main_reload_menu_item"
        app:showAsAction="always"
        app:actionLayout="@layout/reload_action_view"
        android:title="@string/reload_data"/>
</menu>